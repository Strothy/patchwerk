[package]
name = "patchwerk-engine"
version = "0.1.0"
description = "Patchwerk's modular engine: modules, patch graph, cable mapping, fixed-rate tick"
license = "GPL-3.0-only"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
